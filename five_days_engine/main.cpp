#include "SFML/Graphics.hpp"
#include "SFML/Audio.hpp"

#include "Core.h"

int main() {
    setlocale(LC_ALL, "Russian");

    //Core core;

    s.load();
    std::wstring sa;
    Button(sf::Vector2u(0, 0), sa, []() {});
    

    //core.launchGame();

    return 0;
}